import {test, expect, request} from "@playwright/test";
// import {assignment3} from "../utils/fixtures.js";

// assignment3("Hybrid testsing", async({authenticatedPage, createEvent}) => {
//   await authenticatedPage.locator("#nav-events").click();
//   await expect(authenticatedPage.getByText(createEvent)).toBeVisible();
// });